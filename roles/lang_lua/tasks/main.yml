---
- name: Install tar & unzip.
  become: true
  ansible.builtin.package:
    name:
      - tar
      - unzip
    state: present
  notify:
    - uninstall dependencies

- name: "Ensure {{ ansible_env.HOME }}/.local/bin exists."
  ansible.builtin.file:
    path: "{{ ansible_env.HOME }}/.local/bin"
    state: directory
    mode: 0755

- name: Install tools.
  ansible.builtin.include_tasks:
    file: tools.yml
  loop: "{{ tools }}"
  loop_control:
    loop_var: tool
